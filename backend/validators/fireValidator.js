const fireSchema = {
  activeQuery: {}, // Defina o schema real depois
  historyQuery: {}, // Defina o schema real depois
  stateParam: {}, // Defina o schema real depois
};

module.exports = { fireSchema };
